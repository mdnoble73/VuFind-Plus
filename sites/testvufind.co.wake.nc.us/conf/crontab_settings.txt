# Edit this file to introduce tasks to be run by cron.
#
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
#
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').#
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
#
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
#
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
#
# For more information see the manual pages of crontab(5) and cron(8)
#
# m h  dom mon dow   command
# cleanup of tmp files and restart solr to clean up memory

###################
## Crontab setting from a 'configuration' file
## 
## to save current crontab to file: crontab -l > crontab_settings.txt
##
## to set the current crontab from a file : crontab < crontab_settings.txt
##
## added by pascal. 12-11-2014
##################

# !! Please make all changes to the crontab within the git repository system and apply through the above method.
#
# ie :
# git pull origin {current branch}
# sudo crontab < /usr/local/vufind-plus/sites/testvufind.co.wake.nc.us/conf/crontab_settings.txt

###############
# On Reboot
###############
@reboot cd /usr/local/vufind-plus/sites/testvufind.co.wake.nc.us; ./testvufind.co.wake.nc.us.sh start
#@reboot cd /usr/local/vufind-plus/sites/testvufind.co.wake.nc.us; ./extract_and_reindex.sh
# restart continuous re-indexing on reboot. this hasn't been enabled on this server yet. plb 12-22-2014

#######################
# VuFind-Plus Cleanup #
#######################
26 0 * * * cd /usr/local/vufind-plus/sites/testvufind.co.wake.nc.us; ./testvufind.co.wake.nc.us.sh restart
27 0 * * * cd /tmp; rm -rf CURLCOOKIE*
28 0 * * * cd /tmp; rm -rf epb*

###################################
# Data Extract from other systems #
###################################
#  - Takes just a few minutes since we only pull changes
*/5 * * * * cd /usr/local/vufind-plus/vufind/overdrive_api_extract/; java -jar overdrive_extract.jar testvufind.co.wake.nc.us
# extract every five minutes

30 4 * * * /data/vufind-plus/testvufind.co.wake.nc.us/move_export.sh

# Hoopla extract - once a week for the entire server
# No hoopla for Wake County yet
#30 23 * * 0 cd /usr/local/vufind-plus/vufind/cron;./HOOPLA.sh marmot.test

########################
# Update Grouped Works #
########################
00 5 * * * cd /usr/local/vufind-plus/vufind/record_grouping; java -jar record_grouping.jar testvufind.co.wake.nc.us fullRegrouping

###########
# Reindex #
###########
00 6 * * * cd /usr/local/vufind-plus/vufind/reindexer; java -jar reindexer.jar testvufind.co.wake.nc.us fullReindex

##########
# Cron   #
##########
5 */3 * * * cd /usr/local/vufind-plus/vufind/cron/; java -jar cron.jar testvufind.co.wake.nc.us
# run pika cron jobs every three hours

20 0 * * * cd /usr/local/vufind-plus/vufind/cron/; java -jar cron.jar testvufind.co.wake.nc.us DatabaseCleanup
